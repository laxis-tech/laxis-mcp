{
  "name": "laxis-mcpb",
  "version": "0.1.0",
  "private": true,
  "description": "Laxis MCP Bundle for Claude Desktop — stdio bridge to the hosted Laxis MCP server",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "open": "^11.0.0"
  }
}
